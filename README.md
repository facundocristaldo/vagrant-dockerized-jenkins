# vagrant-dockerized-jenkins

This if a setup to create a vagrant virtual machine with a dockerized jenkins
This allow us to create docker images an push them to dockerhub

# vagrant startup

`vagrant up`

